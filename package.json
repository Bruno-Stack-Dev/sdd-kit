{
  "name": "sdd-kit",
  "version": "2.0.0",
  "private": true,
  "description": "Spec-Driven Development kit — engine para Claude Code (zero dependências de runtime).",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "node scripts/sdd-lint.mjs",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "test:py": "python -m pytest .claude/skills"
  }
}
